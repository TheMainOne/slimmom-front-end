import { LayoutStyles } from "./Layout.styled";

const Layout = () => {
  return (
    <LayoutStyles>
      {/* Header */}
      <main>{/* Outlet */}</main>
    </LayoutStyles>
  );
};

export default Layout;
